    lan1_mac macaddr PRIMARY KEY NOT NULL,
    lan1_ip inet,
    rack_location text,
    all_nics_mac macaddr,
    all_nics_chipset text,
    all_nics_bandwidth text,
    bmc_mac macaddr,
    bmc_ip inet,
    bmc_fru_tag text,
    cpu_model text,
    cpu_quality text,
    cpu_current_speed integer,
    cpu_temp integer, 
    memeory_model text,
    memory_quantity text,
    memory_size integer, 
    memory_current_speed integer,
    hdd_models text,
    hdd_quantity text,
    hdd_bandwidth text,
    hdd_iops text,
    gpu_model text,
    gpu_quantity text,
    mb_model text,
    mb_serial text,
    pcie_slot text,
    pcie_device text,
    power_supply_model text,
    power_supply_quantity text,
    power_supply_status text,
    fan_model text,
    fan_quantity text,
    fan_speed integer,
    system_model text,
    system_sn text,
    system_temperature integer,
    system_uid_status text,
    system_power_consumption integer,
    system_location text,
    chassis_model text,
    chassis_sn text,
    ipmi_event text,
    mce_log text,
    bios_version text,
    bios_date date,
    ipmi_firmware_version text,
    ipmi_firmware_date date,
    testing_apps_list text,
    current_running_app text,
    app_starting_time timestamp,
    app_ending_time timestamp,
    app_status text,
    app_result text,
    app_logfile_location text,
    final_result text
